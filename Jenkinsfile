pipeline {
    agent any

    stages {
        stage('Kingsley') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }

        stage('Oby') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }

        stage('Matthew') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }
        
    }
}